<Window x:Class="EventManager.Views.PopUpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EventManager.Views"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="450">
    <Grid Background="AliceBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Viewbox>
            <StackPanel>
                <Label Content="Can't connect to database" HorizontalContentAlignment="Center"/>
                <Label Content="The system will attempt to recconect in:" HorizontalContentAlignment="Center"/>
                <Label x:Name="lbTime" HorizontalContentAlignment="Center"/> 
            </StackPanel>
           
        </Viewbox>
        
        <Button Content="Retry" Grid.Row="1" Margin="10" Click="Button_Click"/>
    </Grid>
</Window>
